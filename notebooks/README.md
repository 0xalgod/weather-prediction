# Notebooks

Notebooks are for exploration and presentation. Production transformations and evaluation logic belong in `src/weather_quant/`. A committed notebook must run from a clean kernel and link to its experiment plan, dataset version, config, and code commit.
