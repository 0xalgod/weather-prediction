# Experiment Registry

This directory stores the machine-readable experiment registry and lightweight run metadata. Human-readable experiment plans live under `docs/experiments/`.

The registry is initialized when the first material research experiment is registered. Required fields are defined in `registry.schema.json`.
